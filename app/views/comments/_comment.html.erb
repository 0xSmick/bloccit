<%= content_tag :div, id: "comment-#{comment.id}" do %>
  <%= content_tag :p, (markdown comment.body) %>
  <% if policy(comment).destroy? %>
    <%= link_to "Delete", comment_url_helper(comment), method: :delete, remote: true %>
  <% end %>
<% end %>
